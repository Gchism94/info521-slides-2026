.DS_Store
*.log

# Quarto build artifacts
_site/
.quarto/
*_files/
/.jupyter_cache/
# _freeze/ is tracked: CI uses cached cell outputs so it does not need to
# re-execute Python. Run `quarto render` locally after changing a .qmd and
# commit both the updated _freeze/ and docs/ together.

# Python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# NOTE: docs/ is intentionally TRACKED (rendered decks for GitHub Pages).

/.quarto/
**/*.quarto_ipynb
